# Server
PORT=8080

# Database (use either DATABASE_URL for cloud DBs like NeonDB, or individual fields for local)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# JWT
JWT_SECRET=your_super_secret_key_here
